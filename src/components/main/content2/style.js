import styled from 'styled-components';

export const BodyMainStyle = styled.div`
    background: var(--background);
    .inner {
        display: flex;
        height: 1000px;
        padding: 93px 0;
    }
    .img-wrap {
        width: 1039px;
        height: 600px;
        overflow: hidden;
    }
`;
