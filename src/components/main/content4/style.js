import styled from 'styled-components';

export const HairMainStyle = styled.div`
    background: var(--background2);
    .inner {
        display: flex;
        height: 1000px;
        padding: 93px 0;
    }
`;
